import { WsAuthWsCustomTooltipDirective } from './ws-auth-ws-custom-tooltip.directive';

describe('WsAuthWsCustomTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new WsAuthWsCustomTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
